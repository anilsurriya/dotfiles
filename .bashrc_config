export HISTCONTROL=ignoreboth:erasedups
export EDITOR=vim
export PATH=$PATH:${HOME}/bin

# Uncomment to start tmux after every bash
#
#if command -v tmux &> /dev/null && [ -n "$PS1" ] && [[ ! "$TERM" =~ screen ]] && [[ ! "$TERM" =~ tmux ]] && [ -z "$TMUX" ];
#  exec tmux
#fi


# Uncomment to set PS1
#
# PS1='[\u@\h \W]\$ '
